# 🚀 Миграция видеопайплайна в Google Cloud

## 📦 Что было сделано

Я подготовил полный набор инструментов для переноса вашего видеопайплайна в Google Cloud с использованием S3 бакета:

### 1️⃣ Новые файлы

- **[video-pipeline/src/storage/s3Input.js](./video-pipeline/src/storage/s3Input.js)** - модуль для работы с S3 входными видео
- **[video-pipeline/src/index-s3.js](./video-pipeline/src/index-s3.js)** - модифицированный пайплайн с поддержкой S3
- **[video-pipeline/config/config.js](./video-pipeline/config/config.js)** - обновлен с настройками S3 input
- **[.env.example](./.env.example)** - шаблон переменных окружения
- **[deploy-to-vm.sh](./deploy-to-vm.sh)** - скрипт для автоматического деплоя

### 2️⃣ Документация

- **[DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md)** - ⭐ **ГЛАВНАЯ ИНСТРУКЦИЯ** - пошаговый гайд по деплою
- **[GOOGLE_CLOUD_SETUP.md](./GOOGLE_CLOUD_SETUP.md)** - настройка Google Cloud VM
- **[S3_SETUP.md](./S3_SETUP.md)** - настройка S3 бакета (GCS, AWS S3, Beget)

---

## 🎯 Быстрый старт

### Шаг 1: Прочитайте главную инструкцию

Откройте **[DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md)** - там все подробно расписано.

### Шаг 2: Создайте S3 бакет

**Вариант A: Используйте Beget (проще, у вас уже есть аккаунт)**

1. Зайдите в панель Beget → Cloud Storage
2. Создайте бакет `video-pipeline-input`
3. Создайте папки: `pending/`, `processing/`, `completed/`, `failed/`

**Вариант B: Используйте Google Cloud Storage**

```bash
gsutil mb -c STANDARD -l europe-west1 gs://video-pipeline-input/
```

### Шаг 3: Создайте VM в Google Cloud

```bash
gcloud compute instances create video-pipeline-vm \
  --zone=europe-west1-b \
  --machine-type=e2-standard-4 \
  --image-family=ubuntu-2204-lts \
  --image-project=ubuntu-os-cloud \
  --boot-disk-size=50GB
```

### Шаг 4: Настройте VM

Подключитесь к VM и установите:
- Node.js 20.x
- Python 3.11 + Whisper
- FFmpeg

Детали в [GOOGLE_CLOUD_SETUP.md](./GOOGLE_CLOUD_SETUP.md) (раздел "Шаг 4")

### Шаг 5: Задеплойте код

**Автоматический способ:**

```bash
# Отредактируйте параметры в скрипте
nano deploy-to-vm.sh

# Запустите деплой
chmod +x deploy-to-vm.sh
./deploy-to-vm.sh
```

**Ручной способ:**

Смотрите раздел "Этап 5" в [DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md)

### Шаг 6: Создайте .env на VM

```bash
# На VM
cd ~/projects/video-pipeline
cp .env.example .env
nano .env
```

Заполните:
```env
USE_S3_INPUT=true
S3_INPUT_BUCKET=video-pipeline-input
S3_INPUT_ENDPOINT=https://s3.ru1.storage.beget.cloud
S3_INPUT_ACCESS_KEY_ID=ваш_ключ
S3_INPUT_SECRET_ACCESS_KEY=ваш_секрет
GOOGLE_API_KEY=ваш_gemini_ключ
PYTHON_EXECUTABLE=/home/USERNAME/whisper-env/bin/python
```

### Шаг 7: Протестируйте

```bash
# Загрузите тестовое видео в S3
aws s3 cp test.mp4 s3://video-pipeline-input/pending/ \
  --endpoint-url=https://s3.ru1.storage.beget.cloud

# На VM: запустите пайплайн
source ~/whisper-env/bin/activate
node video-pipeline/src/index-s3.js
```

### Шаг 8: Настройте автозапуск (опционально)

Создайте systemd service - инструкция в разделе "Этап 9" [DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md)

---

## 📋 Возможности новой версии

### 🔄 Три режима работы

1. **Локальный режим** (как раньше)
   ```bash
   USE_S3_INPUT=false
   node video-pipeline/src/index.js
   ```

2. **S3 режим (по требованию)**
   ```bash
   USE_S3_INPUT=true
   node video-pipeline/src/index-s3.js
   ```
   Обработает все видео из `pending/` папки и завершится

3. **S3 режим с автоматическим мониторингом**
   ```bash
   USE_S3_INPUT=true
   S3_INPUT_ENABLE_POLLING=true
   node video-pipeline/src/index-s3.js --watch
   ```
   Будет проверять `pending/` каждые N секунд и автоматически обрабатывать новые видео

### 📁 Автоматическая организация файлов в S3

```
video-pipeline-input/
├── pending/       ← Загружайте сюда новые видео
├── processing/    ← Видео перемещаются сюда во время обработки
├── completed/     ← Успешно обработанные
└── failed/        ← Видео с ошибками
```

### 🛡️ Надежность

- Автоматический retry при временных сбоях
- Перемещение файлов между папками для отслеживания статуса
- Подробное логирование всех операций
- Очистка временных файлов

---

## 🔧 Архитектура решения

### До (локальная версия):
```
[Локальная папка input/] → [Пайплайн] → [S3 output + БД]
```

### После (облачная версия):
```
[S3 input bucket] → [VM с пайплайном] → [S3 output + БД]
        ↓
    pending/
    processing/
    completed/
    failed/
```

### Преимущества:

✅ **Масштабируемость** - можно обрабатывать любое количество видео
✅ **Автоматизация** - добавили видео в S3 → автоматически обработалось
✅ **Надежность** - VM всегда работает, не зависит от локального компьютера
✅ **Мониторинг** - легко отследить статус каждого видео
✅ **Безопасность** - credentials не хранятся локально

---

## 💡 Рекомендации

### Для начала

1. **Используйте Beget для S3 input** - у вас уже есть аккаунт, это проще
2. **Начните с маленькой VM** (e2-standard-2) для тестов
3. **Протестируйте на одном видео** перед массовой обработкой
4. **Настройте мониторинг** с первого дня

### Для продакшена

1. **Включите автозапуск** через systemd service
2. **Настройте lifecycle rules** для автоудаления старых файлов из `completed/`
3. **Установите alerts** на использование ресурсов VM
4. **Делайте backup** конфигурации и БД

---

## 📊 Примерная стоимость

**Google Cloud VM e2-standard-4 (рекомендуется):**
- VM: ~$120/месяц (24/7)
- Диск 50GB: ~$8/месяц
- Трафик: обычно в пределах бесплатного лимита

**Beget Cloud Storage:**
- Хранение: ~1₽/GB/месяц
- Операции: минимальны для входного бакета

**Общая оценка: ~$130-150/месяц**

💰 **Экономия:**
- Используйте Spot VM → скидка до 80%
- Останавливайте VM когда не используется
- Автоудаление файлов из S3 через 30 дней

---

## 🆘 Что делать, если возникли проблемы

1. **Проверьте логи:**
   ```bash
   tail -f ~/projects/video-pipeline/pipeline.log
   ```

2. **Проверьте S3 подключение:**
   ```bash
   node test-s3.js
   ```

3. **Проверьте systemd service:**
   ```bash
   sudo systemctl status video-pipeline.service
   sudo journalctl -u video-pipeline.service -n 100
   ```

4. **Смотрите раздел "Устранение неполадок"** в [DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md)

---

## 📚 Документация

- **[DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md)** ⭐ - полная инструкция от А до Я
- **[GOOGLE_CLOUD_SETUP.md](./GOOGLE_CLOUD_SETUP.md)** - детали по настройке VM
- **[S3_SETUP.md](./S3_SETUP.md)** - все про S3 бакеты (GCS, AWS, Beget)

---

## ✅ Чеклист миграции

- [ ] Прочитал [DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md)
- [ ] Создал S3 бакет с папками
- [ ] Создал VM в Google Cloud
- [ ] Установил Node.js, Python, FFmpeg на VM
- [ ] Настроил Whisper
- [ ] Задеплоил код на VM
- [ ] Создал .env с реальными credentials
- [ ] Протестировал на одном видео
- [ ] Настроил systemd service для автозапуска
- [ ] Настроил мониторинг

---

## 🚀 Следующие шаги после миграции

1. **API endpoint** - можно добавить REST API для загрузки видео
2. **Webhook notifications** - уведомления о завершении обработки
3. **Horizontal scaling** - несколько VM для параллельной обработки
4. **Kubernetes** - если понадобится большая масштабируемость

---

**Удачи с миграцией! Если возникнут вопросы - смотрите детальную документацию выше** 🎉
