# 🚀 Быстрый старт

## Предустановка (один раз)

### 1. Установите Ollama

**Windows:**
1. Скачайте с https://ollama.ai/download/windows
2. Установите Ollama
3. Откройте терминал и выполните:

```bash
ollama pull phi3
```

**macOS:**
```bash
# Установите Ollama
brew install ollama

# Запустите сервис
ollama serve

# В новом терминале загрузите модель
ollama pull phi3
```

**Linux:**
```bash
# Установите Ollama
curl -fsSL https://ollama.ai/install.sh | sh

# Запустите сервис
ollama serve

# В новом терминале загрузите модель
ollama pull phi3
```

### 2. Убедитесь что Ollama запущен

```bash
# Проверьте что Ollama работает
curl http://localhost:11434/api/version
```

Должны увидеть что-то типа: `{"version":"0.x.x"}`

### 3. Установите зависимости Node.js

```bash
npm install
```

## ✅ Проверка готовности системы

Запустите проверку настроек:

```bash
npm run check
```

Вы должны увидеть:
```
✓ Ollama is running
✓ phi3 model is available
✓ Database connected successfully
✓ Database is responsive
✓ FFmpeg is available

✓ All systems ready!
```

Если есть ошибки - следуйте инструкциям на экране.

## 🎬 Первый запуск

### Шаг 1: Добавьте тестовое видео

Поместите ваше первое видео в папку input:

```bash
# Windows
copy C:\path\to\your\video.mp4 video-pipeline\input\

# macOS/Linux
cp /path/to/your/video.mp4 video-pipeline/input/
```

**Важно:** Начните с короткого видео (30-60 секунд) для быстрого теста!

### Шаг 2: Запустите пайплайн

```bash
npm start
```

### Что произойдет:

1. **Извлечение аудио** (~10 секунд для 1 минуты видео)
   ```
   [Step 1/5] Extracting audio...
   Processing: 100% done
   ✓ Audio extracted successfully
   ```

2. **Транскрипция с Whisper** (~30-60 секунд для 1 минуты аудио)
   ```
   [Step 2/5] Transcribing with Whisper...
   Loading Whisper model... (первый раз - загрузка модели)
   Transcription completed
   ```

3. **Анализ текста** (~5-10 секунд)
   ```
   [Step 3/5] Analyzing text with Ollama...
   Text analysis completed
   ```

4. **Генерация упражнений** (~10-15 секунд)
   ```
   [Step 4/5] Generating exercises...
   Generated 4 exercises
   ```

5. **Сохранение в БД** (~1 секунда)
   ```
   [Step 5/5] Saving to database...
   Video data saved to database with ID: 1
   ```

### Результат:

```
✓ Video processed successfully!
  - Database ID: 1
  - Output JSON: c:\...\video-pipeline\output\video.json
  - CEFR Level: B1
  - Topics: Technology, Education
  - Exercises: 4
```

## 📊 Просмотр результатов

### 1. JSON файл

Откройте файл в папке `video-pipeline/output/`:

```bash
# Windows
notepad video-pipeline\output\your-video.json

# macOS
open video-pipeline/output/your-video.json

# Linux
cat video-pipeline/output/your-video.json
```

### 2. База данных

Подключитесь к БД и проверьте:

```sql
-- Посмотреть все обработанные видео
SELECT id, video_name, cefr_level, topics
FROM video_learning_content;

-- Посмотреть конкретное видео с данными
SELECT * FROM video_learning_content WHERE id = 1;
```

## 🔄 Обработка нескольких видео

Просто добавьте несколько видео в папку input и запустите:

```bash
npm start
```

Пайплайн обработает все видео последовательно и покажет итоговую статистику:

```
PROCESSING SUMMARY
══════════════════════════════════════════════════════════

Total videos: 5
✓ Successful: 4
✗ Failed: 1
```

## 🛠 Частые проблемы при первом запуске

### ❌ "Cannot connect to Ollama"

**Решение:**
```bash
# Запустите Ollama в отдельном терминале
ollama serve

# Проверьте что работает
curl http://localhost:11434/api/version
```

### ❌ "phi3 model NOT found"

**Решение:**
```bash
ollama pull phi3
```

### ❌ "Database connection failed"

**Решение:**
Проверьте настройки в [`video-pipeline/config/config.js`](video-pipeline/config/config.js):
```javascript
database: {
  host: 'mgodag3j.beget.tech',
  port: 3306,
  user: 'mgodag3j_english',
  password: 'Mais19970619',
  database: 'mgodag3j_english'
}
```

### ❌ Whisper очень медленно работает

**Первый запуск:** Whisper загружает модель (~500MB). Это нормально и происходит один раз.

**Последующие запуски:** Если всё равно медленно, попробуйте меньшую модель:
- Откройте [`video-pipeline/src/transcription/whisper.js`](video-pipeline/src/transcription/whisper.js)
- Замените `'Xenova/whisper-small'` на `'Xenova/whisper-tiny'`

### ❌ "out of memory"

Убедитесь что у вас достаточно RAM:
- Минимум 8GB для Whisper small
- Минимум 4GB для Whisper tiny
- Ollama + phi3 требует ~2GB

## 📈 Следующие шаги

1. ✅ Протестируйте с коротким видео (30-60 сек)
2. ✅ Проверьте результаты в JSON и БД
3. ✅ Попробуйте с несколькими видео
4. ✅ Настройте темы в [`config.js`](video-pipeline/config/config.js) под ваши нужды
5. ✅ Интегрируйте с вашим бэкендом (см. [USAGE.md](USAGE.md))

## 📚 Дополнительные ресурсы

- [README.md](README.md) - полная документация
- [USAGE.md](USAGE.md) - примеры использования и интеграции
- [video-pipeline/config/config.js](video-pipeline/config/config.js) - настройки

## 🆘 Нужна помощь?

Запустите диагностику:
```bash
npm run check
```

Это покажет что именно не работает и как это исправить.

---

**Готово!** 🎉

Теперь вы можете обрабатывать видео для вашего приложения по изучению английского языка!
